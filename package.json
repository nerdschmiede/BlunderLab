{
  "name": "blunderlab",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "chess.js": "^1.4.0",
    "chessground": "^9.2.1"
  },
  "devDependencies": {
    "vite": "^5.4.21",
    "vitest": "^2.0.0",
    "gh-pages": "^6.1.1"
  }
}
